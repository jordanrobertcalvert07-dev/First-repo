{
  "name": "@lifelike/core",
  "version": "0.1.0",
  "description": "UI-agnostic core for LifeLike: design tokens, sun/theme engine, schema, and AI capture contract. Shared by every platform.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "suncalc": "^1.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/suncalc": "^1.9.2",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  },
  "license": "MIT"
}
